/**
 * Format a timestamp as a user-friendly time string.
 * @param {number} timestamp - Unix timestamp.
 * @returns {string} - Formatted time string (e.g., "2 hours ago").
 */
function formatTime(timestamp) {
  // Your time formatting logic here
  // You can use libraries like moment.js to help with time formatting
}

export default formatTime;
